# Model weights

The trained DenseNet121 checkpoint is generated by:

```bash
python scripts/train.py
```

It will be written to `models/densenet121_ham10000.pt` and is intentionally ignored by git because the binary can be large. For a portfolio repository, attach the checkpoint through GitHub Releases, Git LFS, or a model-hosting service and update the README link.
